"""Versioned Pydantic models with semver support.

Modules:
    | pydantic_semver_annotation: Pydantic type for semver ``Version``.
    | versioned_model: Base model with ``data_model_version`` and optional
        coercion.
"""
