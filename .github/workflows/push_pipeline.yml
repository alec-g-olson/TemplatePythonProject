name: Makefile CI - Push

on:
  push:
    branches: [ "main" ]

jobs:
  push:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - run: git checkout HEAD^

    - name: Push artifact
      run: make push
